// Re-export the IndexerLoadData type from the SDK so both the snapshot loader
// and the main railgun module can reference it without circular imports.
export type { IndexerLoadData } from "@kohaku-eth/railgun";
