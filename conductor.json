{
  "scripts": {
    "setup": "if ! command -v bun >/dev/null; then echo 'bun is required (https://bun.sh)'; exit 1; fi; if [ ! -d node_modules/next ] || [ ! -d node_modules/typescript ]; then bun install --frozen-lockfile; else echo 'Dependencies already installed'; fi",
    "run": "dotenvx run -f .env.tianjin -- bun run dev -- --developer-mode"
  }
}
